import joi = require('joi');
import { AnySchema, StringSchema, ValidationOptions } from 'joi';

export { joi };

export function objectId(): StringSchema;

export function registrationNumber(): StringSchema;

export function validateSchema<T>(value: T, schema: AnySchema, _?: null, options?: ValidationOptions): T;
