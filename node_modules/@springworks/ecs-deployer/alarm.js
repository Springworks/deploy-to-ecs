'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.create = create;

var _lodash = require('lodash');

var _ecsCreator = require('./ecs-creator');

function create(alarm, policy_arn) {
  const cloudwatch = (0, _ecsCreator.createCloudWatch)();
  const alarm_with_actions = (0, _lodash.merge)({}, alarm, { AlarmActions: [policy_arn] });
  return cloudwatch.putMetricAlarm(alarm_with_actions).promise();
}